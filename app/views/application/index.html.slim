// locals: collection, filters, display_keys, pagination
//- collection = @collection
//- filters = @filters
//- display_keys = @display_keys
//- pagination = @pagination


h2= controller_name.camelize

= render partial: "filter_form", locals: {controller: controller_name.to_sym, filters: filters}
= render partial: "list_table", locals: {display_keys: display_keys, collection: collection, filter_keys: filters.keys, pagination: pagination}

