= form_group(:name, text_field_tag(:name, params[:name], size: 70, onchange: ajax_search(:name, table)))
= form_group(:category, select_tag_with_options(:category, options: { onchange: ajax_search(:category, table)}))
= form_group(:nation, select_tag_with_options(:nation, options: { onchange: ajax_search(:nation, table)}))

