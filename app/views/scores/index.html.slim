- keys = [:sid, :competition_name, :category, :segment, :date, :result_pdf, :ranking, :skater_name, :nation, :tss, :tes, :pcs, :deductions]
- controller.register_list_record_callback(:result_pdf){|record, key| link_to_pdf(record[key]) }
- controller.register_list_record_callback(:sid){|record, key| link_to(record.id, controller: :scores, action: :show, sid: record.sid)  }

- collection = @collection
- filters = @filters


////////////////////////////////////////////////////////////////
h2= controller_name.camelize

= render partial: "filter_form", locals: {controller: controller_name.to_sym, filters: filters}
= render partial: "list_table", locals: {keys: keys, collection: collection, filter_keys: filters.keys }


